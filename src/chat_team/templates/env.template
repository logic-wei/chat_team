# 凭证已统一到 config.yaml — 请将下方值迁移到 config.yaml 的对应字段。
# 此文件中的环境变量仍会被读取（作为 fallback），但建议统一到 config.yaml。
#
# WECOM_BOT_ID=       → config.yaml bots[].bot_id
# WECOM_SECRET=       → config.yaml bots[].secret
# OPENAI_API_KEY=     → config.yaml llm.api_key
# OPENAI_BASE_URL=    → config.yaml llm.base_url
